use stdlib.logic;

problem RelationGraphIndependentSet {
  set V;
  relation Edge(u: V, v: V);

  find Pick : Subset(V);

  must forall (u, v) in Edge:
    indicator(Pick.has(u)) + indicator(Pick.has(v)) <= 1;

  maximize count(v in V where Pick.has(v));
}
